{
  "enableAllProjectMcpServers": true,
  "permissions": {
    "defaultMode": "plan",
    "allow": [
      // Nix
      "Bash(nix develop:*)",
      "Bash(nix build:*)",
      "Bash(om ci)",

      // Read files
      "Bash(rg:*)"
    ]
  }
}
