# Shared preferences, consumed by each desktop's adapter. This is not a module.
{
  keyboardLayout = "us";
  keyboardOptions = "ctrl:nocaps";
  naturalScrolling = true;
  touchpadScrollFactor = 0.5;
}
